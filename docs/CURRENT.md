# 当前交接快照

> 快照日期：2026-08-07
> 当前分支：`prototype/pnr-formation-loop`
> 远程分支：`origin/prototype/pnr-formation-loop`
> 最近已推送提交：`bb138dc`（记录 F00 提交前纠偏门）
> 稳定行为基线：`c4ada7d`（Policy 阶段收口）

这份文件只回答“现在在哪里、接下来做什么”。阶段历史和决策原因分别放在[进度账本](./2026-08-06-generalization-goals-plan-progress.md)与[决策索引](./DECISIONS.md)。任何 agent 接手前仍须重新运行 `git status --short`；这里的工作树列表只是快照，不是覆盖本地事实的许可。

## 一句话状态

S01–S08、G01–G08、P00–P03 已封存；F00 固定偏移形成挡拆的标准回放已经通过用户首次人眼判断，提交前纠偏实现与自动门现已通过。当前只等待用户复验两个交接瞬间；尚未批准 F01、随机起手或任何新战术。

## 已经确定的事实

- F00 使用显式 opt-in 的 `form_pnr`；缺省仍为 `preset_pnr`，不得改变旧 S/G/P 世界。
- 标准 right/left F00 动画连续，用户已经确认视觉方向正确；这项结论保留，不推倒重做。
- 独立审计结论为“GO WITH CORRECTIONS”：原实现的自动门通过，但它过度绑定固定 tick、分支与终局，没有覆盖若干负向语义。
- 审计最小反例包括：交接时 D1 比 O1 距篮筐更远约 `0.13m`；合法但更远的等待点可在 O1 尚距目标约 `1.02m`、速度约 `1.72m/s` 时被 O5 的 `screen_set` 提前带入挡拆；use/reject gate 对调曾能通过输入校验。
- 当前工作树已经针对这些反例完成联合就绪、D1 篮筐侧、语义 PASS、固定模板/拓扑拒绝和 planner 信息裁剪；本快照实测 `npm test` 57/57、lint、build 全部通过。
- 已推送的 `bb138dc` 只包含路线文档，不包含 F00 行为代码。

## 当前工作树所有权

接手时必须重新核对，当前快照下：

| 路径 | 当前用途 | 处理原则 |
| --- | --- | --- |
| `lib/pnr-core.ts` | F00 Formation 状态、观察、计划、运动与交接纠偏 | 属于未提交的 F00 行为工作，不得回退 |
| `lib/pnr-strategy.ts` | Formation 决策阶段与零 adjustment 接缝 | 属于未提交的 F00 行为工作 |
| `lib/pnr-f00-formation.ts` | F00 固定配置、回放与审计 | 新文件，尚未提交 |
| `tests/pnr-core.test.mjs` | F00 语义门与全部旧回归 | 当前 57/57 通过，尚未提交 |
| `components/PnrLab.tsx` | F00 观察入口 | 只读 UI，尚未提交 |
| `AGENTS.md`、`README.md`、`docs/README.md`、`docs/CURRENT.md`、`docs/DECISIONS.md` | 渐进式 agent 接手结构 | 属于文档整理工作 |

工作树中的 `jointReady`、`formation_ready`、固定模板校验、team-specific landmark view 和按篮筐方向计算 D1 目标已经接入测试。固定 tick、固定 `USE/SWITCH`、固定终局和防守读取完整 landmarks 不再是 F00 的通过条件。它们仍是未提交行为，不能因为自动门通过就跳过用户复验或与本次文档整理混成不明提交。

## 已完成但尚未提交的 F00 纠偏

1. **联合就绪。** `screen_set` 只表示 O5 到位并减速；O1 还必须进入等待区域、速度合格且合法持球。双方只能在联合就绪事件的下一合法边界进入旧 PnR。
2. **D1 篮筐侧责任。** D1 Formation 目标从 O1→篮筐方向推导，继续负责 O1、处于篮筐侧且比 O5 更接近 O1；D5 继续负责 O5，不提前换防。
3. **语义 PASS。** 验证联合就绪、责任几何、形成阶段无提前 branch/effective/impeded、无远程掩护和 next-tick 因果。固定 tick、分支、覆盖与终局只作诊断。
4. **固定模板与最小拓扑。** F00 只接受批准模板；拒绝非标准 offsets、gate 对调、等待点/身体或路线冲突及越界。
5. **最小信息所有权。** 防守规划器只能读取公共 screen anchor/形成信息，不能读取 O1 waiting/use/reject 几何；UI 可以通过独立观察对象全量展示。

以上五项已经通过当前自动门。不要顺手实现更一般的局部坐标系、动态路线、随机位置或新防守原语。

## 自动验收结果

本快照实际运行结果：

- `npm test`：57/57 通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- 测试同时覆盖旧 170 输入、G08 manifest、全部 P 策略、right/left 确定镜像、速度/身体/球权/角色，以及五项 F00 负向语义门。
- 修正后的合法分支、覆盖或终局不再被首次标准回放答案锁死。

## 用户只需要复验的两个瞬间

1. O5 设稳时：O1 是否已经停稳等待，D1 是否确实在 O1 篮筐侧，D5 是否仍跟 O5。
2. 下一 tick 交接时：动作是否连续、无抖动、无远程效果，随后旧挡拆阅读是否自然发生。

当前下一动作就是把这两个瞬间交用户复验。用户确认后，先明确划分 F00 行为文件与本次文档结构文件的提交范围，再 commit/push，然后停止。

## 当前明确不做

- F01–F03。
- 随机位置、拖拽、自动选边、手动控制。
- 沉退、追过、ICE、夹击、外弹或其他新篮球原语。
- 投篮、犯规、篮板、完整比赛、3v3/5v5。
- ML/RL、旧项目迁移或生产级 UI。
- 重新开启 P04 或改写 G08 manifest。

## 完成交接时必须更新

下一位完成当前任务的 agent 应在同一变更中更新：

- 顶部最近提交与状态；
- 工作树所有权表；
- 自动门实际结果；
- 用户是否完成短回放复验；
- 下一停止点是否仍为“F00 提交并停止”。
