# AGENTS.md

本文件适用于整个仓库，并应保持短小。详细规则必须放入 spec/ 下的权威专项规格，不得复制到这里。

## 1. 授权边界

- 当前仓库处于 M0 规格阶段。
- 编辑、整理或讨论规格，不代表获准编写业务代码、创建脚手架、安装依赖或进入 M1。
- 只有用户明确要求开始实现时，才能进行开发动作。
- 不得从“未来会开发”推导出“现在开始开发”的授权。

## 2. 最小阅读路径

开始任务时：

1. 阅读 spec/README.md；
2. 阅读始终必读的产品边界规格；
3. 只读取路由表中与当前任务直接相关的专项规格；
4. 不得默认加载全部规格或根 README.md。

若修改跨越多个层，必须读取所有受影响层的权威规格。

## 3. 不可破坏的项目边界

- 产品是二维篮球战术空间推演器，不是完整比赛、篮球游戏或物理引擎。
- 权威球员状态保持最小：稳定 ID、阵营、二维位置和当前角色。
- 基础状态没有速度、朝向、身体、疲劳、比分、比赛时间、进攻时间和犯规。
- 内部逻辑步只表达先后，不代表现实秒数。
- 战术描述目标、角色、约束和允许动作，不播放完整固定轨迹。
- 进攻和防守都必须能够根据空间变化重新判断。
- 核心必须无 UI、无网络、无 AI 服务也能运行。
- AI 输出是受校验提案，不得直接修改权威状态或执行任意代码。

详细定义以产品边界规格及对应专项规格为准。

## 4. 规格修改规则

- 同一事实只允许一个权威文件拥有；其他文件使用链接，不复制全文。
- 产品范围修改产品边界规格。
- 运行时语义修改架构规格。
- 字段和版本修改数据契约规格。
- AI 权限和上下文修改 AI 规格。
- 验收和测试修改验证规格。
- 若两份规格冲突，停止修改并先解决权威归属。

## 5. 实现期规则

仅在用户明确授权实现后适用：

- 先写或更新相关验收，再修改行为；
- 随机行为必须使用显式种子；
- UI 和 AI 不得直接写权威状态；
- 新动作通过公共接口和注册表接入，不在主循环写特例；
- 不得以未来需求为由提前加入 3v3、5v5、投篮结果、人体物理或在线 AI 控制；
- 完成前运行与改动相关的类型、schema、测试和架构检查。

