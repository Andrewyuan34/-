# AGENTS.md

本仓库是一个独立、短周期的 2v2 挡拆算法实验台。当前批准范围为网页场景 S01–S08、G01–G08 受限域泛化检查点、已验收的 P00 策略输入契约与 P01“错位攻击优先”进攻策略，以及当前 P02–P03 防守策略和有限对局矩阵增量；根目录 `README.md` 是运行方式与最短边界说明。

## 不可破坏的内核边界

- 架构保持为进攻队级规划器、防守队级规划器、中立世界与运动解析器。
- 两队只读取公开世界事实和自队职责，不得读取对方隐藏计划。
- 中立解析器只处理固定步长运动、局部几何、球权、传球飞行、触球归属和事件，不替任一球队评分或选方案。
- 世界以 1/60 秒固定步推进；重规划只由事件、终止条件或有限看门狗触发，并保留最短承诺与滞回。
- `contact`、`route_exposure`、`impeded`、`screen_effective` 必须保持独立；没有接触或合理移动走廊时不得产生远程阻挡。
- 所有场景使用稳定公开输入和确定性回归检查，不得把计划、胜负、接球者或终局作为模拟输入。

## 当前范围

- 只做 O5 在 O1 右侧或其严格镜像左侧设掩护的标准 2v2；右侧是唯一规范战术坐标，左右侧不得形成两套算法。
- 当前目录保留 S01–S08；新增场景只能追加，不得覆盖旧场景。
- G01 只能用现有计划与评分扫描公开速度输入；不得加入 PlanId、战术分支、scenarioId 判断或速度点特判。
- G02 只能扫描现有 D1 绕前反应成本并观察真实触球；预计 ETA 不得直接指定最终触球者，也不得加入 delay 点特判。
- G03 只能扫描现有 D1 接球后恢复成本；必须分别记录防守选择 `DIG_POST`、D5 真实进入局部协防半径、O5 观察后选择 `KICK_OUT` 三层，不得用防守意图直接开放分球或加入 delay 点特判。
- G04 后模拟内核必须从公开 `initialPositions` 创建四人世界；`cue` 只允许存在于场景适配层，不得进入核心配置、规划器观察或方案选择。
- G05 必须保留原始 33 点账本：31 个合法输入完整双运行，`O1.y/-0.24` 与 `D1.y/+0.24` 明确记录为 `EXPECTED_INVALID_INITIAL_OVERLAP` 并继续由 G04 在世界创建前拒绝；不得静默跳过、缩放偏移、调整评分或增加点位特判。
- G06 只能组合已经通过单轴门的速度、绕前/恢复时间和三个已验收位置；只检查确定性、合法性、阶段共同前缀和几何解释，不硬编码笛卡尔积答案，也不加入组合专用逻辑。
- G07 必须通过统一 tactical/local frame 共享候选、评分、阈值、角色和事件解析；左侧必须是真实世界状态，不得镜像 Canvas、复制 left planner、增加左右专用阈值或方向结果补丁。
- 未经用户逐轮确认，不加入投篮结果、更多战术覆盖、3v3、5v5、ML/RL、旧项目迁移、持久化、登录或生产化。
- 视觉入口是可丢弃观察壳，不得反向修改模拟状态。

## 修改与验证

- 行为改动先补或同步更新紧凑的因果/不变量检查。
- 场景定义集中在 `lib/pnr-scenarios.ts`，模拟内核集中在 `lib/pnr-core.ts`，网页只消费公开快照。
- 完成前运行 `npm test`、`npm run lint` 和 `npm run build`。
- 每轮只解决一个可观看的篮球问题，达到第一个明确停止点后暂停，交给用户亲眼判断。

## G08 冻结规则

- G08 的冻结核心基线是 `d92ed9f63bba3dcfa28d65d54b6a47c7e3e1f74c`；锁定 manifest commit 是 `b994d2d7520f00069e011e629ccb54f90ba8c5d5`，内容哈希是 `sha256:5ff19401f264663f5d5fcfc2e944cb1e0ef4430093a11281193f7f8d8d07fee3`。
- G08 已在冻结核心上首次揭示并通过 24/24 双运行与核心不变量，代表动画也已由用户验收；审计、测试与网页入口提交为 `669701b6d98222f3705b51b20a37fec4346f728b`。
- `lib/pnr-g08-heldout-manifest.ts` 及其 24 个输入继续保持锁定。用户已批准从该检查点进入 P00；后续架构增量必须把全部 G08 输入作为普通回归，不能改写 manifest 或按 held-out case ID 补丁。
- held-out 失败时保留冻结核心与 manifest，报告最早失败及最小轨迹并停止；不得针对 case ID、side 或同一批输入调参后重跑。

## P00 策略契约规则

- P00 检查点只注册 `OFFENSE_BALANCED_READ@1` 与 `DEFENSE_BALANCED_COVERAGE@1`，两套默认 profile 的 adjustment 永远保持 0；后续策略不得改变默认选择或 P00 旧行为指纹。
- 进攻规划器只能读取进攻策略，防守规划器只能读取防守策略；中立世界创建、运动与事件解析完全不接收策略。
- 评分顺序必须是硬可行性、`baseScore`、本队 `strategyAdjustment`、`effectiveScore`。策略只重排可行候选，绝不能恢复被 veto 的候选。
- 正式场景与 G01–G08 审计输入显式携带双方默认策略 ID/version；选择与 profile 构造后深拷贝并冻结。
- 播放或单步后策略锁定，暂停不解锁；只有重置并创建新回合才解锁。
- P00 前保存的 170 个合法输入逐 tick 行为指纹必须全部保持。P00 已由用户验收并以 `31aad3c2819ebda861bd101dfe9832869622c7b9` 推送。

## P01 进攻策略规则

- P01 只新增 `OFFENSE_MISMATCH_PRESSURE@1`；P01 版本点的防守仍只有 `DEFENSE_BALANCED_COVERAGE@1`，且不得改变 PlanId、基础评分、阈值或运动目标。
- 唯一非零偏好是 `offense_mismatch` / post-switch 阶段硬可行 `ATTACK_BIG` 的全局 `+0.02`；不得读取 scenarioId、速度点、side、held-out ID 或结果标签。其他候选与阶段必须为 0。
- `baseScore` 与硬可行性先于策略；被 veto 的 `ATTACK_BIG` 始终保持 `effectiveScore = null`。选择 ATTACK 后必须由同一队级计划同时分配 O1 `attack_big` 与 O5 `clear_lane`。
- 3.98m/s 必须形成 Balanced FEED / Pressure ATTACK；3.72m/s 两者都保持 FEED；4.00m/s 两者都可 ATTACK。左右侧共享同一常数和逻辑。
- 默认 Balanced 的 170 个旧输入逐 tick 指纹继续不变；Pressure 必须在同一 170 个批准输入上双运行确定、合法且无信息泄漏。G08 manifest 不得修改。
- 网页运行前可在两套进攻策略间选择；播放或单步后锁定，暂停不解锁，重置新回合才解锁。P01 已完成人眼验收并作为独立版本点收口；P02–P03 不得改写其 `+0.02` 校准、170 输入回归或已批准回放。
